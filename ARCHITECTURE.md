# Architecture

The server's architecture is documented in
**[`docs/architecture/README.md`](docs/architecture/README.md)** — the entrypoint &
startup, the WebSocket bridge, the catalogue/dispatch pipeline, `discover_tools`, the
GDScript LSP client, the registry consumer, and the contract surface, with Mermaid
diagrams throughout.

It is also rendered at
<https://npgamedev.github.io/godot-mcp-server/architecture/>.
